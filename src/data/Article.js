class Article {
    constructor(objectID, title, url, author, num_comments, points){
         this.objectID = objectID;
         this.title = title;
         this.url = url;
         this.author = author;
         this.num_comments = num_comments;
         this.points = points;
    }
}

export default Article;